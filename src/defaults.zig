pub const memory_max_messages: usize = 20;
pub const memory_max_facts: usize = 64;
pub const tools_enabled: bool = true;
pub const file_read_enabled: bool = true;
pub const file_write_enabled: bool = true;
pub const schedule_tools_enabled: bool = true;
pub const tool_max_rounds: usize = 4;
pub const tool_output_max_bytes: usize = 8192;
pub const shell_timeout_ms: u64 = 5000;
pub const telegram_poll_timeout_seconds: u32 = 20;
pub const telegram_rate_limit_per_minute: u32 = 20;
pub const websocket_host = "127.0.0.1";
pub const websocket_port: u16 = 8765;
pub const websocket_path = "/ws";
pub const websocket_rate_limit_per_minute: u32 = 20;
pub const daemon_interval_seconds: u32 = 60;
pub const heartbeat_interval_seconds: u32 = 30 * 60;
pub const timezone_offset_minutes: i32 = 0;
